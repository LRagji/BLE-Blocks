import React from "react";
import { Headline } from 'react-native-paper';

export class Blocks extends React.Component {

    render(): React.ReactNode {
        return (
            <Headline>List of blocks</Headline>
        );
    }
}